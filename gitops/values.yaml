# aws | local — set by the root Application's helm parameter (gitops/bootstrap).
# Later specs gate AWS-only child Applications (Karpenter, AWS LB Controller,
# external-dns, EBS CSI, RDS) on this value being "aws" (spec 021 Req 3).
target: aws
