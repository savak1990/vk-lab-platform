apiVersion: v1
kind: ServiceAccount
metadata:
  annotations:
    # No explicit wave defaults to "0", after the Job's hook at wave -2 -
    # the Job's pod creation then fails looking up this ServiceAccount.
    argocd.argoproj.io/sync-wave: "-3"
  name: envoy-gateway-webhook-probe
  namespace: envoy
